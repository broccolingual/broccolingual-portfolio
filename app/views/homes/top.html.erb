<% @blogs.each do |blog| %>
  <div class="article">
    <%= link_to blog_path(blog) do %>
      <h2><%= blog.title %></h2>
      <p class="body"><%= blog.body.truncate(128) %></p>
      <p class="created-at"><%= blog.created_at.strftime('%Y/%m/%d') %></p>
    <% end %>
  </div>
<% end %>
